name: linux
run-name: test
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: List files in the repository
        uses: actions/checkout@v4
      - run: |
          ls ${{ github.workspace }}